This is plain text that should be translated.

This line has `inline code` that should not be translated.

This line has $x^2 + y^2$ math that should not be translated.

This costs $5 dollars (not math).

```
This entire block
should not be translated
even with $math$ and `code`
```

This is more translatable text.

$$
\int_0^\infty e^{-x} dx = 1
$$

Final translatable paragraph.
